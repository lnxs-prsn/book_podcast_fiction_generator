# QWEN.md

Read `HANDOFF.md` (repo root) first — it points to the current dated handoff
in `progress/`, the read-first order, and a trust map of which documents are
live vs stale. All binding agent rules are in `AGENTS.md`; they apply to
Qwen sessions too.

Quick invariants: interpreter is `.venv/bin/python` from the repo root
(uv-managed — never pip); `fiction_loop/CONTRIBUTING.md` before changing
anything under `fiction_loop/`; `fiction_loop/core/agent_conduct.md` binds
during chapter runs; pathspec-limited commits; no paid API calls unless
explicitly budgeted; this machine is a Raspberry Pi.
