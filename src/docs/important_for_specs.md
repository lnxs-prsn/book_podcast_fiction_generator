Contracts – function signatures, module boundaries, error types

Business logic – domain rules (vocabulary gates, touch semantics, concept coverage)

Edge case specs – missing files, API failures, interrupts, disk full

data schema specs – the exact format of every persisted file (JSON structure, markdown sections, naming conventions)