# decisions.md — owner decision ledger (APPEND-ONLY; copy to your state/docs dir)

Rules: entries are never edited after the owner answers — corrections are NEW
entries superseding old ones by reference. An agent whose work triggers an
owner-level decision (taste, money, autonomy, hard-to-reverse) writes a
PROPOSAL below and STOPS; only an explicit owner answer unblocks. A session
may surface a pending proposal but must not act on it.

---

## <ISO timestamp> — <short title>  [PROPOSED]
Essence: <what is being decided, once, in plain words>
Analogy: <a human-world parallel that makes the stakes felt>
Impact + reversibility: <how far it reaches; can it be undone, at what cost;
  which option is the one-way door>
Architecture points at: <which option the system's existing design favors,
  and why — cite the pattern, not a preference>
Context: <facts, numbers, links to evidence>
Options:
  A) <option>                                                    ← DEFAULT
  B) <option>
Decision: <left blank until the owner answers; then e.g.
  "A — owner chose it explicitly <date>" and the header tag becomes
  [ACCEPTED] / [REJECTED]>
